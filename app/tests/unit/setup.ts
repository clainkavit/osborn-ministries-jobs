// M1 unit tests cover pure functions (permissions) and Zod schemas
// (validation) -- no DOM rendering yet, so no jest-dom matchers needed.
// This file is a placeholder the config points at; component-render tests
// in a later milestone will add "@testing-library/jest-dom/vitest" here
// (and install @testing-library/dom as its peer).
export {};
